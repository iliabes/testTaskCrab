import type { Direction, RealTheme } from './types';
export declare function updateBodyClassName({ theme, nativeScrollbar, className, prevClassName, }: {
    theme: RealTheme;
    nativeScrollbar?: boolean;
    className?: string;
    prevClassName?: string;
}): void;
export declare function updateBodyDirection(direction: Direction): void;
