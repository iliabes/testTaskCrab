import React from 'react';
import type { Direction, RealTheme, Theme } from './types';
export interface ThemeProviderProps extends React.PropsWithChildren<{}> {
    theme?: Theme;
    systemLightTheme?: RealTheme;
    systemDarkTheme?: RealTheme;
    direction?: Direction;
    nativeScrollbar?: boolean;
    scoped?: boolean;
    rootClassName?: string;
}
export declare function ThemeProvider({ theme: themeProp, systemLightTheme: systemLightThemeProp, systemDarkTheme: systemDarkThemeProp, direction: directionProp, nativeScrollbar, scoped: scopedProp, rootClassName, children, }: ThemeProviderProps): React.JSX.Element;
export declare namespace ThemeProvider {
    var displayName: string;
}
