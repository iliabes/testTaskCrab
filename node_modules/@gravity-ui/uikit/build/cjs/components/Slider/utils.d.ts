import type { SliderProps as RcSliderProps } from 'rc-slider';
import type { SliderInnerState, SliderProps } from './types';
export declare function getMarksFromInfoPoints({ infoPointsCount, max, min, }: {
    infoPointsCount: number;
    min: number;
    max: number;
}): RcSliderProps['marks'];
export declare function getInnerState({ max, min, availableValues, defaultValue, marksCount, step, value, }: {
    max: number;
    min: number;
    availableValues: SliderProps['availableValues'];
    defaultValue: SliderProps['defaultValue'];
    marksCount: SliderProps['marksCount'];
    step: SliderProps['step'];
    value: SliderProps['value'];
}): SliderInnerState;
