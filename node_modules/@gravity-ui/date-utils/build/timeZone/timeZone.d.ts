import type { TimeZone } from '../typings';
/**
 * Returns the user's time zone.
 */
export declare const guessUserTimeZone: () => string;
/**
 * Returns all time zones.
 */
export declare const getTimeZonesList: () => string[];
export declare function isValidTimeZone(zone: string): boolean;
export declare function timeZoneOffset(zone: TimeZone, ts: number): number;
export declare function normalizeTimeZone(input: string | undefined, defaultZone: string): string;
export declare function fixOffset(localTS: number, o: number, tz: string): [timestamp: number, offset: number];
