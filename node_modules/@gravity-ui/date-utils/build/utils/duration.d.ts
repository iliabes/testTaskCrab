import type { DateTimeInput, DurationUnit } from '../typings';
export interface DurationObject {
    milliseconds: number;
    days: number;
    months: number;
}
export declare function getDuration(amount: DateTimeInput, unit?: DurationUnit): DurationObject;
