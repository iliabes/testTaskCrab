import React from 'react';
import './OnboardingMenuItem.css';
export type OnboardingMenuItemProps = {
    title: string;
    status?: 'completed' | 'pending';
    defaultExpanded?: boolean;
    children?: React.ReactNode;
    buttons?: React.ReactNode;
    hasDivider?: boolean;
    loading?: boolean;
};
export declare const OnboardingMenuItem: ({ title, status, children, defaultExpanded, buttons, hasDivider, loading, }: OnboardingMenuItemProps) => React.JSX.Element;
